<%= form_for @welcome,:url => (@new ? generic_create_path(@welcome.class) : generic_update_path(@welcome.class,@welcome.id)) do |f| %>
  <% if @welcome.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@welcome.errors.count, "error") %> prohibited this welcome from being saved:</h2>

      <ul>
      <% @welcome.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if !params[:process_id].nil? %>
    <%= hidden_field_tag :process_id, params[:process_id] %>
    <%= hidden_field_tag :seq, params[:seq] %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
