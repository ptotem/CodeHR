<h1>Listing approval_mats</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @approval_mats.each do |approval_mat| %>
  <tr>
    <td><%= link_to 'Show', approval_mat %></td>
    <td><%= link_to 'Edit', edit_approval_mat_path(approval_mat) %></td>
    <td><%= link_to 'Destroy', approval_mat, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Approval mat', new_approval_mat_path %>
