<p id="notice"><%= notice %></p>

<div class="row-fluid">
  <div class="offset1 span6">
    <div class="table-header">
      Rating Scale Details :-
    </div>
    <table class="table table-striped table-bordered table-hover">
      <tbody>
          <tr>
            <th>Name</th>
            <td><%= @rating_scale.name %></td>
          </tr>
          <tr>
            <th>Description</th>
            <td><%= @rating_scale.description %></td>
          </tr>
      </tbody>
    </table>
  </div>
</div>

<%= link_to 'Edit', edit_rating_scale_path(@rating_scale) %> |
<%= link_to 'Back', rating_scales_path %>
