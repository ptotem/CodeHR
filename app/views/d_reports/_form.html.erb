<%= form_for(@d_report) do |f| %>
  <% if @d_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@d_report.errors.count, "error") %> prohibited this d_report from being saved:</h2>

      <ul>
      <% @d_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
