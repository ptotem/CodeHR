<h1>Listing d_reports</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @d_reports.each do |d_report| %>
  <tr>
    <td><%= link_to 'Show', d_report %></td>
    <td><%= link_to 'Edit', edit_d_report_path(d_report) %></td>
    <td><%= link_to 'Destroy', d_report, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New D report', new_d_report_path %>
