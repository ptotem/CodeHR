<%= form_for(@pms_group_category) do |f| %>
  <% if @pms_group_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pms_group_category.errors.count, "error") %> prohibited this pms_group_category from being saved:</h2>

      <ul>
      <% @pms_group_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
