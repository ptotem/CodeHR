<h1>Listing pms_group_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pms_group_categories.each do |pms_group_category| %>
  <tr>
    <td><%= pms_group_category.name %></td>
    <td><%= link_to 'Show', pms_group_category %></td>
    <td><%= link_to 'Edit', edit_pms_group_category_path(pms_group_category) %></td>
    <td><%= link_to 'Destroy', pms_group_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pms group category', new_pms_group_category_path %>
